version: "3.9"

services:
  # ── Observability ──────────────────────────────────────────
  jaeger:
    image: jaegertracing/all-in-one:1.57
    container_name: jaeger
    ports:
      - "16686:16686"   # UI
    environment:
      COLLECTOR_OTLP_ENABLED: "true"

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.101.0
    container_name: otel-collector
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ./observability/otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - "4317:4317"     # OTLP gRPC (services → collector)
      - "8889:8889"     # Prometheus scrape endpoint
    depends_on:
      - jaeger

  prometheus:
    image: prom/prometheus:v2.52.0
    container_name: prometheus
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    depends_on:
      - otel-collector

  grafana:
    image: grafana/grafana:10.4.3
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - ./observability/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus
      - jaeger

  # ── Inference ─────────────────────────────────────────────
  triton:
    image: nvcr.io/nvidia/tritonserver:24.05-py3
    container_name: triton
    command: ["tritonserver", "--model-repository=/models", "--log-verbose=0"]
    volumes:
      - ./triton-models:/models
    ports:
      - "8000:8000"   # HTTP
      - "8001:8001"   # gRPC
      - "8002:8002"   # Prometheus metrics
    deploy:
      resources:
        limits:
          memory: 4G

  # ── Business Services ──────────────────────────────────────
  pedestrian:
    image: kaikanghuang/pedestrian-service:triton
    container_name: pedestrian-svc
    ports:
      - "50052:50052"
    environment:
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      TRITON_URL: triton:8001
      POSE_MODEL: yolov8s-pose
    depends_on:
      - otel-collector
      - triton
    deploy:
      resources:
        limits:
          memory: 512M

  vehicle:
    image: kaikanghuang/vehicle-service:otel
    container_name: vehicle-svc
    ports:
      - "50053:50053"
    environment:
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
    depends_on:
      - otel-collector
    deploy:
      resources:
        limits:
          memory: 1G

  router:
    image: kaikanghuang/router-service:triton
    container_name: router-svc
    ports:
      - "50051:50051"
    environment:
      PEDESTRIAN_SERVICE_ADDR: pedestrian:50052
      VEHICLE_SERVICE_ADDR: vehicle:50053
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      TRITON_URL: triton:8001
      DET_MODEL: yolov8s
      PED_CHANNELS: "2"
      PED_CROP_CHUNK_SIZE: "32"
      PED_CROP_PADDING_RATIO: "0.15"
    depends_on:
      - pedestrian
      - vehicle
      - otel-collector
      - triton
    deploy:
      resources:
        limits:
          memory: 512M

  gateway:
    image: kaikanghuang/gateway:crop
    container_name: gateway-svc
    ports:
      - "5000:5000"
    environment:
      ROUTER_SERVICE_ADDR: router:50051
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
    depends_on:
      - router
      - otel-collector
